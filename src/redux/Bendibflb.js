var Bendibflb = ( (state={},action)=>{
    switch (action.type) {
       
        case "GEDANLIEBISOS":  //歌单播放列表
            let arr2={};
            arr2 = action.text
        return arr2;
   
        default:
          return state;
    }
})
export default Bendibflb;