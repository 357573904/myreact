var Usergedan = ((state={},action)=>{
    switch (action.type) {
    
         case "USEsssRGEDAN":  //用户歌单名
            let arr1={};
            arr1 = action.text
            return arr1;
        default:
          return state;
    }

})
export default Usergedan;